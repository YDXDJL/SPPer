# 冠影守望者：笔记本部署与演示指南

> 展示包不包含 YOLO 模型、训练代码或推理环境。四组视频直接使用既有逐帧识别数据，
> 由 OpenCV 在 CPU 上同步解码，因此不要求 NVIDIA 显卡或 CUDA。

## 1. 展示包内容

- `computer/`：监控网页、后台服务和预构建静态网页；
- `demo_assets/vision_replays/`：四组原视频、JSONL与摘要；
- `main/`、`band/`、`swimmer/`：三个硬件端的当前固件源码；
- `scripts/`：安装、验收、启动和停止脚本；
- `DATA_INTERFACE_SPEC.md`、`项目全面介绍.md`：接口与项目说明。

不包含：`yolo/`、`.git/`、`.venv/`、`node_modules/`、`.next/`、`.pio/`及其他构建缓存。

## 2. 笔记本要求

- Windows 10/11 64位；
- 64位 Python 3.11或3.12，并在安装时勾选 `Add Python to PATH`；
- Chrome或Edge；
- 至少2GB可用磁盘空间；
- USB口用于RGB网关；
- 演示穿戴设备时，笔记本与设备必须处于同一局域网。

展示包已经包含 `computer/out`，普通展示不需要安装 Node.js。只有修改网页源码并重新构建时，
才需要 Node.js 20.9或更高版本并在 `computer/` 下运行 `npm install`、`npm run build:static`。

## 3. 首次安装

将整个 `SPPer-展示版` 文件夹复制到笔记本本地磁盘（推荐，例如 `C:\SPPer-展示版`），
不要直接长期从移动硬盘运行，以避免盘符变化或USB瞬断。打开PowerShell：

```powershell
cd C:\SPPer-展示版
powershell -ExecutionPolicy Bypass -File .\scripts\setup_laptop.ps1
powershell -ExecutionPolicy Bypass -File .\scripts\verify_demo.ps1
```

默认优先使用包内 `offline_wheels/` 离线安装。若离线包不兼容或希望联网更新：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\setup_laptop.ps1 -Online
```

## 4. 启动与停止

启动：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\start_demo.ps1
```

浏览器会打开 `http://127.0.0.1:8000/`。开发/配对页为
`http://127.0.0.1:8000/dev/`。停止后台：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\stop_demo.ps1
```

如果脚本启动失败，可在前台查看完整日志：

```powershell
cd .\computer
.\.venv\Scripts\python.exe run.py
```

## 5. 现场连接顺序

1. 启动笔记本，关闭会占用8000端口的旧进程；
2. 接入RGB网关，确认网页显示串口已识别、协议就绪；
3. 确认笔记本网络设为“专用网络”，首次弹窗允许Python通过专用网络防火墙；
4. 启动救生员手环和泳者端，等待网页设备数更新；
5. 在开发页完成泳者与设备配对；只有一台泳者端在线时，视频演示会自动对应ID1；
6. 回到监控页依次检查 `oneline`、`twolines`、`crashing`、`drowning`；
7. 实体救生按钮会真实驱动装置，触发前必须确认周围安全。

## 6. 四组CPU演示

- `oneline`：单泳者与灯号映射；
- `twolines`：双泳者正常通行；
- `crashing`：第125帧碰撞开始暂停，第166帧结束暂停；
- `drowning`：第335帧疑似溺水暂停，第490帧高风险暂停。

暂停期间维持当时的灯光、手环预警与泳者振动，点击“继续播放”才前进。演示状态不会
自动发送真实救生指令；真实触发与复位只能使用网页上的独立按钮。

## 7. 网络与串口排查

- 网页显示RGB网关为0：确认USB线支持数据、在设备管理器找到COM口，再在开发页点重新连接；
- 手环/泳者端连Wi-Fi但连不到电脑：确认处于同一网段，允许Python防火墙访问，并访问
  `http://笔记本IPv4:8000/` 验证局域网可达；
- 8000端口被占用：运行 `scripts\stop_demo.ps1`，或重启笔记本后再启动；
- 视频清单为空：运行 `scripts\verify_demo.ps1`，根据SHA256结果重新复制损坏文件；
- 串口盘符变化无需改代码，电脑会逐个端口探测；开发页仍可手动选择。

## 8. 展示前验收清单

- [ ] `verify_demo.ps1`通过；
- [ ] 四组视频均可选择并显示画面；
- [ ] `crashing`两次事件暂停正确；
- [ ] `drowning`第335/490帧提示正确且不自动触发救生；
- [ ] RGB网关帧序号与ACK持续增长；
- [ ] 手环和泳者端均显示在线；
- [ ] 配对关系正确；
- [ ] 实体救生装置已复位，周围无阻挡；
- [ ] 移动硬盘保留一份未改动备份。
